# Database Configuration
DATABASE_URL=sqlite:./monitor.db

# Server Configuration
SERVER_PORT=5400

# Metrics Collection
COLLECTION_INTERVAL=60

# Authentication Credentials (replace with secure values)
AUTH_USERNAME=servers
AUTH_PASSWORD=Soft@26*

# Optional: HTTP Timeout Configuration (in seconds)
HTTP_TIMEOUT=60

# Optional: Use PostgreSQL instead of SQLite
# DATABASE_URL=postgresql://username:password@localhost/monitor_db

# Optional: Log level
# RUST_LOG=info
